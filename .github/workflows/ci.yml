name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Lint and Test
    runs-on: ubuntu-22.04

    strategy:
      fail-fast: false
      matrix:
        toolchain: [stable, nightly]

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.toolchain }}
          components: rustfmt, clippy

      - name: Cache
        uses: Swatinem/rust-cache@v2

      - name: Formatting
        run: cargo --locked fmt --all -- --check

      - name: Linting
        run: cargo --locked clippy --all-targets --all-features -- -D warnings

      - name: Tests
        id: run_ci
        run: cargo --locked xtask ci

      - name: Upload snapshots of failed tests
        if: ${{ failure() && steps.run_ci.outcome == 'failure' }}
        uses: actions/upload-artifact@v7
        with:
          name: failed-snapshots-${{ matrix.toolchain }}
          path: "**/snapshots/*.snap.new"
          if-no-files-found: ignore
          retention-days: 7
